import * as THREE from 'three'

export default {
  background: {
    color: 0xefefef
  },
  camera: {
    defaultPos: new THREE.Vector3(0, 1.5, 5)
  },
  controls: {
    defaultTarget: new THREE.Vector3(0, 1.67, 0)
  }
}